import { VerticalBox } from "std-widgets.slint";
export component ProcessPage inherits Window {
    
    VerticalBox {
        // search bar
        
        // process list
        // (pid) (name) (cpu) (ram) (action)
    }
    
}